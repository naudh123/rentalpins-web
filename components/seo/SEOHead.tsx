/**
 * SEO helpers for App Router pages.
 * Use `buildPageMetadata()` from `@/lib/seo/metadata` in `export const metadata` or `generateMetadata`.
 * Use schema components (FAQSchema, BreadcrumbSchema, StructuredData) in page JSX.
 */
export { buildPageMetadata, type PageSeoInput } from "@/lib/seo/metadata";
