# Copy to .env.local — never commit real secrets.
# Local dev: leave NEXT_PUBLIC_BASE_PATH unset → http://localhost:3000/

# --- Deploy target ---
# staging | production
# Local dev: production-like (no staging banner). Use staging only for /staging deploys.
NEXT_PUBLIC_DEPLOY_ENV=production
# Hosted staging only: NEXT_PUBLIC_BASE_PATH=/staging and NEXT_PUBLIC_DEPLOY_ENV=staging
# NEXT_PUBLIC_BASE_PATH=/staging

# --- Site ---
# Local app URL (optional). Production/Vercel: set to https://www.rentalpins.com — never localhost (breaks sitemap in GSC).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Optional override for share links and SEO when SITE_URL is localhost locally:
# NEXT_PUBLIC_PUBLIC_SITE_URL=https://www.rentalpins.com

# --- Firebase client (public) — use your Firebase project values ---
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX
# Optional: web push (Firebase Cloud Messaging)
# NEXT_PUBLIC_FIREBASE_VAPID_KEY=your-vapid-key

# --- Google Maps ---
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# --- GA4 ---
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# --- Auth ---
# Web post rollout: allow Google sign-in + form contact without OTP (also enables activate/chat for owners).
NEXT_PUBLIC_ALLOW_UNVERIFIED_OWNER_CONTACT=true
# Set true to force OTP before post (legacy). When false, ALLOW_UNVERIFIED is enabled automatically.
NEXT_PUBLIC_REQUIRE_PHONE_VERIFICATION=false
# Local dev: set true + add fictional test numbers in Firebase Console (Phone provider)
# NEXT_PUBLIC_FIREBASE_PHONE_AUTH_TESTING=true

# --- Firebase Admin (server-only) ---
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# --- Cron (server-only) — Vercel Cron: Authorization: Bearer $CRON_SECRET ---
CRON_SECRET=

# --- Property agent (server-only) — /api/agent/chat, map copilot, /advisor chat ---
OPENAI_API_KEY=
# Optional: defaults to gpt-4o-mini
# AI_AGENT_MODEL=gpt-4o-mini
# AI_DAILY_MESSAGE_LIMIT=40
# AI_AGENT_MAX_STEPS=8
# AI_EMBEDDING_MODEL=text-embedding-3-small
# AGENT_INDEX_SECRET=your_random_secret_for_blog_rag_indexing
# MCP_API_KEY=your_mcp_api_key
# MCP endpoint: https://www.rentalpins.com/api/mcp

# Showcase high-intent lead alerts (Resend) — /developers agent only
# Same keys as RudderTech contact form (https://resend.com/api-keys)
# RESEND_API_KEY=re_...
# LEAD_NOTIFICATION_EMAIL=admin@rentalpins.com
# For testing: RentalPins <onboarding@resend.dev>
# After verifying rentalpins.com in Resend:
# EMAIL_FROM=RentalPins <admin@rentalpins.com>
